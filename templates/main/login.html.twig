{% extends 'base.html.twig' %}

{% block title %}
    Proyely - Iniciar Sesión
{% endblock %}

{% block body %}
    <div class="container mt-5">
        <div class="container text-center">
            <img src="{{ asset('images/logo.png') }}" alt="Logo" class="img-fluid" style="max-width: 100px;">
        </div>
        <div class="row justify-content-center">
            <div class="col-md-6">
                <h1 class="text-center mb-4">Iniciar Sesión</h1>

                {% if error %}
                    <div class="alert alert-danger">
                        {{ error.messageKey|trans(error.messageData, 'security') }}
                    </div>
                {% endif %}

                <form action="{{ path('security_signin') }}" method="post">
                    <div class="form-group mb-3">
                        <label for="username">Usuario</label>
                        <input type="text" id="username" name="_username" class="form-control" placeholder="Usuario" value="" required {% if not error %}autofocus{% endif %}>
                    </div>

                    <div class="form-group mb-3">
                        <label for="password">Contraseña</label>
                        <input type="password" id="password" name="_password" class="form-control" placeholder="Contraseña" required {% if error %}autofocus{% endif %}>
                    </div>

                    <div class="form-group form-check mb-3">
                        <input type="checkbox" id="remember_me" name="_remember_me" class="form-check-input">
                        <label class="form-check-label" for="remember_me">Recordarme</label>
                    </div>

                    <button type="submit" class="btn btn-primary w-100">Entrar</button>
                </form>
            </div>
        </div>
    </div>
{% endblock %}
