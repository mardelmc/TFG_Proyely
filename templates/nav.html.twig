{% set current_path = app.request.get('_route')|split('_') %}
<nav class="navbar fixed-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('main') }}"><i class="fa-solid fa-building-shield"></i> Proyely/a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {{ current_path[0] == 'main' ? 'active" aria-current="page' }}" href="{{ path('main') }}">
                        <i class="fa-solid fa-home"></i> Home
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ current_path[0] == 'mission' ? 'active" aria-current="page' }}" href="{{ path('mission_list') }}">
                        <i class="fa-solid fa-clipboard"></i> Missions
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="fa-solid fa-person-military-rifle"></i> Enforcers
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="#">
                        <i class="fa-solid fa-power-off"></i> Exit
                    </a>
                </li>
            </ul>
        </div>
    </div>
</nav>
